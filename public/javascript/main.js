//delete job from character, will do this later maybe
// <span class="btn btn-sm btn-danger deleteJob">Remove</span>
// $(".deleteJob").click((event) => {
	// character.jobs.splice(character.jobs.findIndex(job => job.name === this.name), 1);
	// $(this).parent().remove();
	// event.stopPropagation();
// });